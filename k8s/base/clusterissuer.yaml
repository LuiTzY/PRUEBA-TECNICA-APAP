apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    # Servidor de Let's Encrypt producción
    server: https://acme-v02.api.letsencrypt.org/directory
    # Tu email para notificaciones de certificado
    email: luisdavidmarte00@gmail.com
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
      - http01:
          ingress:
            class: nginx